<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- ====================================== -->
        <!-- UPDATE KYC EXPIRY STATUS               -->
        <!-- ====================================== -->
        <record id="cron_update_kyc_expiry" model="ir.cron">
            <field name="name">Finance: Update KYC Expiry Status</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code">model._cron_update_kyc_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
            <field name="priority">5</field>
        </record>

        <!-- ====================================== -->
        <!-- UPDATE AUM FOR ALL CLIENTS             -->
        <!-- ====================================== -->
        <record id="cron_update_aum" model="ir.cron">
            <field name="name">Finance: Update AUM for All Clients</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code">
# Process in batches of 100 to avoid timeout
partners = model.search([('is_finance_client', '=', True)])
batch_size = 100
for i in range(0, len(partners), batch_size):
    batch = partners[i:i+batch_size]
    batch.action_update_aum()
    model.env.cr.commit()  # Commit after each batch
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
            <field name="priority">10</field>
        </record>
        
    </data>
</odoo>
